# Register/enable from this dir: `infra register && infra enable`.
[[service]]
name = "vllm-rerank"
role = "backend"
reach = "host"
kind = "systemd"
exec_start = "./serve.py rerank"
working_dir = "../.."
port = 8004
environment = { VLLM_SERVER_DEV_MODE = "1" }
