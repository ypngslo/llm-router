[project]
name = "llm-router"
version = "0.1.0"
description = "Self-hosted LLM stack: LiteLLM proxy + host vLLM model servers. Owns its own vllm runtime."
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.1",
    "vllm>=0.22.1",
]
